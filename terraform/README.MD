# Terraform for infrastracture provision


## Install module 
under reach env , run `terraform get` to install module

## Init the terraform project
`terraform init`

## Dry run the terraform code
`terraform plan`

## apply the terraform code
`terraform apply`
